package oo.heranca.desafio;

public interface Esportivo { // interface não possui um corpo, ou seja, definições que estão dentro de chaves
	// A interface serve caso nao sabe onde implementar, mas quer eventualmente decidi implementar o metodo em algima classe 
	
	void ligarTurbo(); 
	void desligarTurbo();

	// Quando a void quer dizer que nao retorna a nenhum paramentro Return, geralmente é usado o If,Else
}
